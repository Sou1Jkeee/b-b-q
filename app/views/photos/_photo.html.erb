<div class="event-photo buffer-both">
  <%= link_to image_tag(photo.photo.thumb.url, class: 'img-thumbnail'), photo.photo.url, data: {lightbox: 'image', title: "Добавил: #{ link_to photo.user.name, photo.user }"} %>
  Добавил: <%= render partial: 'users/user', object: photo.user %>
</div>
